# Автоматические метки GitLab для Code Review

## Что нового?

Теперь при экспорте комментариев code review в GitLab issues автоматически добавляются метки на основе **приоритета** и **категории** комментария.

## Как использовать?

### 1. Настройте базовые метки (опционально)

В настройках VS Code добавьте метки по умолчанию:

```json
{
  "code-review.gitlab.defaultLabels": ["code-review"]
}
```

### 2. Создайте комментарий с приоритетом и категорией

При создании комментария укажите:
- **Приоритет**: Низкий (1), Средний (2) или Высокий (3)
- **Категория**: например, `security`, `performance`, `nitpick`, `refactor-suggestion`

### 3. Экспортируйте в GitLab

Используйте команду **"Code Review: Export to GitLab"**

### 4. Результат

Задача в GitLab получит метки:

| Приоритет | Метка GitLab |
|-----------|--------------|
| 1 (Низкий) | `minor` |
| 2 (Средний) | `major` |
| 3 (Высокий) | `critical` |

Плюс метка из категории (например, `security`)

## Пример

**Комментарий:**
- Приоритет: 3 (Высокий)
- Категория: `security`
- Базовые метки: `["code-review"]`

**Результат в GitLab:**
```
Метки: code-review, critical, security
```

**Примечание:** Категории автоматически нормализуются:
- `Security` → `security`
- `Potential Issue` → `potential-issue`
- `Refactor Suggestion` → `refactor-suggestion`

## Визуализация в GitLab

Рекомендуется настроить цвета меток в вашем проекте GitLab:

### Приоритеты:
- 🔴 `critical` - Красный
- 🟠 `major` - Оранжевый  
- 🟡 `minor` - Желтый

### Категории (примеры):
- 🔴 `security` - Темно-красный
- 🔵 `nitpick` - Синий
- 🟡 `refactor-suggestion` - Желтый
- 🟢 `performance` - Зеленый

## Преимущества

✅ **Автоматизация** - Метки добавляются автоматически  
✅ **Фильтрация** - Легко фильтровать задачи по приоритету  
✅ **Визуализация** - Цветные метки для быстрой оценки  
✅ **Интеграция** - Используйте метки в GitLab CI/CD  

## Дополнительная информация

Подробная документация:
- [docs/gitlab-labels-ru.md](docs/gitlab-labels-ru.md) - Полное руководство на русском
- [docs/gitlab-integration-setup.md](docs/gitlab-integration-setup.md) - Настройка интеграции с GitLab
