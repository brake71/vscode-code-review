# Автоматическая генерация меток GitLab

## Обзор

При экспорте комментариев code review в GitLab issues, расширение автоматически добавляет метки (labels) на основе приоритета и категории комментария.

## Как это работает

### 1. Метки по умолчанию

Вы можете настроить базовые метки, которые будут добавляться ко всем задачам:

```json
{
  "code-review.gitlab.defaultLabels": ["code-review", "bug"]
}
```

### 2. Метки приоритета

Автоматически добавляются на основе приоритета комментария:

| Приоритет | Значение | Метка GitLab |
|-----------|----------|--------------|
| Низкий    | 1        | `minor`      |
| Средний   | 2        | `major`      |
| Высокий   | 3        | `critical`   |
| Не указан | 0        | *(не добавляется)* |

### 3. Метки категории

Значение категории комментария нормализуется и добавляется как метка:
- Преобразуется в нижний регистр
- Пробелы заменяются на дефисы

Примеры преобразования:
- `Nitpick` → `nitpick`
- `Potential Issue` → `potential-issue`
- `Refactor Suggestion` → `refactor-suggestion`
- `Security` → `security`
- `Performance` → `performance`

## Пример

Комментарий со следующими параметрами:
- **Приоритет**: 3 (Высокий)
- **Категория**: `security`
- **Метки по умолчанию**: `["code-review", "bug"]`

Получит следующие метки в GitLab:
```
["code-review", "bug", "critical", "security"]
```

## Преимущества

1. **Фильтрация** - Легко фильтровать задачи по приоритету или категории в GitLab
2. **Группировка** - Группировать задачи по меткам для лучшей организации
3. **Автоматизация** - Создавать автоматические workflow на основе меток
4. **Визуализация** - Быстро видеть приоритет и тип задачи по цветным меткам

## Настройка меток в GitLab

Для лучшей визуализации рекомендуется настроить цвета меток в вашем проекте GitLab:

### Метки приоритета:
- `critical` - 🔴 Красный (#FF0000)
- `major` - 🟠 Оранжевый (#FFA500)
- `minor` - 🟡 Желтый (#FFFF00)

### Метки категории:
- `nitpick` - 🔵 Синий (#0000FF)
- `potential-issue` - 🔴 Красный (#FF0000)
- `refactor-suggestion` - 🟡 Желтый (#FFFF00)
- `security` - 🔴 Темно-красный (#8B0000)
- `performance` - 🟢 Зеленый (#00FF00)

## См. также

- [GitLab Integration Setup Guide](gitlab-integration-setup.md)
- [GitLab Integration Guide](gitlab-integration-guide.md)
